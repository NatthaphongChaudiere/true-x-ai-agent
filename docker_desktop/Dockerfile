# 1. Use the official n8n image as the base (the plain pizza)
FROM n8nio/n8n:latest

# 2. Switch to 'root' user to install software
USER root

# 6. Switch back to the 'node' user so n8n runs safely
USER node